"""TriageGuard product sub-agents.

Each sub-agent lives under its own subpackage (`agents.reproducibility`,
`agents.root_cause`, ...). System prompts are loaded from
`.claude/prompts/` at runtime; do not inline them here.
"""
